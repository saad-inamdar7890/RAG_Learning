# Ingestion Acceptance Criteria

A document is marked "approved" only if:
- Text extraction captures all major sections without missing blocks
- Tables are preserved or converted into readable text
- Each chunk can be traced to a document + page number
- The source URL is recorded
